1、
一般需求测试：
看需求文档、原型图、宣讲视频
写测试用例（测试用例有一定格式，因为云效只能接收特定格式）
和产品、开发宣讲测试用例，由他们评审
根据线上和本地化不同的环境，进行测试环境发布（线上是测试环境-预发布环境-正式环境）（本地化是本地化测试环境-本地化正式环境）
测完测试环境要进行验收，验收完才可以发正式环境
后续需要一直支持正式环境中的bug
2、
自动化测试场景补全：
由于是老场景，缺少接口文档
需要先把所有接口抓出来，根据一般用户的操作流程
构建一个顺序的接口调用场景，后调用的接口常依赖于前面的接口
然后把这些接口先每个接口做一个测试用例，保证先跑起来，后面再补充更详细的测试用例
测试用例要符合测试框架的要求，要求是符合特定格式的yaml文件，还有一些环境变量要配置
