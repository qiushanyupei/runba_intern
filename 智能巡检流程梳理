1.巡检计划管理：
1.1巡检计划管理主页面：
1.1.1导入按钮是GET请求，用来查询，返回导入记录                 /patrolScheme/import/list
1.1.2搜索按钮是GET请求，用来查询，返回巡检计划列表记录          /patrolScheme/list
1.1.3编辑按钮是GET请求，用来编辑，返回要修改的单个巡检计划      /patrolScheme/edit
1.1.4删除按钮是无请求，只是进入一个确认删除弹窗
1.2导入计划界面
1.2.1导入和导入列表按钮是GET请求，用来查询，返回导入的记录      /patrolScheme/import/list
1.2.2返回列表按钮是GET请求，用来查询，返回巡检计划列表记录      /patrolScheme/list
1.3编辑巡检计划界面
1.3.1开始上传按钮是POST请求，用来上传文件，返回文件名、绝对路径、相对路径、文件大小         /file/upload
1.3.2保存按钮是POST请求                                      
1.4删除弹窗界面
1.4.1确定按钮是一个POST请求，传入删除的计划id，返回删除成功与否的"message"，和status_code    /patrolScheme/del
1.5导入计划->导入页面
1.5.1”巡检计划导入模板下载“按钮是GET请求，用来下载文件
1.5.2“导入说明下载”按钮是GET请求，用来下载文件
1.5.3选取文件按钮是无请求，用来选择本地文件，估计是记录本地文件地址？                      /file/upload
1.5.4开始上传“按钮”是POST请求，用来上传文件
1.5.5导入“按钮”是POST请求，请求体包含文件路径，会返回一个message，表示上传是否成功            /patrolScheme/import
    导入后还会自动跳转到导入列表             /patrolScheme/import
1.6导入计划->导入列表界面
1.6.1下载“按钮”是GET请求，返回阿里云存好的之前上传导入的文件
1.6.2错误导出“按钮”是GET请求
2.巡检路径管理
2.1巡检路径管理主页面：
2.1.1
