1.巡检计划管理：
1.1巡检计划管理主页面：
1.1.1导入按钮是GET请求，用来查询，返回导入记录
1.1.2搜索按钮是GET请求，用来查询，返回巡检计划列表记录
1.1.3编辑按钮是GET请求，用来编辑，返回要修改的单个巡检计划
1.2导入计划界面
1.2.1导入和导入列表按钮是GET请求，用来查询，返回导入的记录
1.2.2返回列表按钮是GET请求，用来查询，返回巡检计划列表记录
1.3编辑巡检计划界面
1.3.1开始上传按钮是POST请求，用来上传文件，返回文件名、绝对路径、相对路径、文件大小
